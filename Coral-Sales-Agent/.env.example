# Sales Agent Environment Configuration

# Core Agent Settings
CORAL_SSE_URL=http://localhost:5555/sse
CORAL_AGENT_ID=sales_agent
MODEL_NAME=gpt-4o-mini
MODEL_PROVIDER=openai
MODEL_API_KEY=your_openai_api_key
MODEL_TEMPERATURE=0.1
MODEL_MAX_TOKENS=8000
TIMEOUT_MS=60

# CRM Integration (Optional - Agent works without CRM)
CRM_ENABLED=false
CRM_TYPE=salesforce
CRM_API_KEY=your_crm_api_key
CRM_ENDPOINT=https://your-crm-instance.salesforce.com

# Supported CRM Types:
# - salesforce
# - hubspot  
# - pipedrive
# - custom (for webhook integrations)

# Agent Features
# - Stateless operation (no database required)
# - Works for enterprise and individual users
# - Plug-and-play CRM integration
# - Coordinates with specialized agents for:
#   * Prospect discovery (Firecrawl + OpenDeepResearch)
#   * Lead qualification (VoiceInterface + BANT scoring)
#   * Outreach coordination (VoiceInterface + Email)
#   * Sales insights (Pandas analytics)